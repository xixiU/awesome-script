# HTML5视频播放工具

## 功能特性

- ✅ 支持 `<video>` 和 `<audio>` 元素倍速控制
- ✅ 可视化倍速UI (拖动滑块 + 预设按钮)
- ✅ 视频截图、画中画、缓存
- ✅ 全屏、网页全屏
- ✅ 字幕服务
- ✅ 万能网页全屏

## 快捷键

### 倍速控制
| 按键 | 功能 |
|------|------|
| **Q** | 显示/隐藏倍速面板 |
| **E** | 加速 +0.1x |
| **C** (YouTube用V) | 加速 +0.1x |
| **X** (抖音用S) | 减速 -0.1x |
| **Z** (抖音用A) | 1.0x ↔ 记忆速度 |

### 其他功能
| 按键 | 功能 |
|------|------|
| **P** | 视频截图 |
| **I** | 画中画 |
| **M** | 缓存视频 |
| **S** | 字幕服务 |
| **N** | 下一集 |
| **F / Enter** | 全屏 |
| **Shift+Enter** | 网页全屏 |
| **←→** | 快进/快退 5秒 |
| **Shift+←→** | 快进/快退 20秒 |
| **↑↓** | 音量调节 |
| **D** | 上一帧 |
| **G** (YouTube用E) | 下一帧 |
| **Space** | 暂停/播放 |
| **ESC** | 退出全屏 |

## 倍速UI使用

### 显示面板
```
按 Q 键
```

### 调节速度
- **预设按钮**: 0.5x / 0.75x / 1.0x / 1.25x / 1.5x / 2.0x
- **拖动滑块**: 0.25x - 4.0x 精确调节
- **键盘微调**: E键 +0.1x, X键 -0.1x

### 特性
- 3秒自动隐藏
- 鼠标悬停保持显示
- 实时速度同步

## 更新记录

### v2.2.3 (2026-09-13)
- ✨ 新增: **初始自动选择最高清晰度**（油猴版 + 扩展版）
  - 仅在视频加载时设置一次，**不做定时轮询**，之后用户可自由手动切换而不会被覆盖
  - **自动跳过需要会员/付费的档位**（识别 VIP/大会员/Premium 文案、皇冠角标、`aria-disabled` 等特征），宁可停在次高清也不点进付费墙
  - 优先走播放器 JS API（YouTube `getAvailableQualityLevels`、hls.js `levels`、dash.js），拿不到再退回点击站点清晰度菜单
  - 已适配 pornhub / bilibili / 爱奇艺 / 优酷 / 芒果TV / 西瓜视频 / A站，其余站点走 `data-quality` 等通用特征兜底
  - 直播不处理（切清晰度常触发重连）；开关：油猴菜单「初始自动选最高清晰度」/ 扩展 popup「播放设置」
- 🐛 修复: 扩展版在 pornhub 等站点**右键"在新标签页打开视频"多开标签页后，切过去视频不播放、必须刷新**
  - 根因：「反失焦暂停」保护层在 `document_start` 无条件把 `document.hidden` 伪装为 `false`，并把站点的 `visibilitychange` 监听整个吞掉。后台标签页加载时播放器以为可见而立即起播，但浏览器实际拦截后台标签页播放；播放器赖以重试起播的 `visibilitychange` 又收不到，于是永久卡在初始状态
  - 修复：保护改为**延迟武装**——页面真实可见之后（且等站点处理完那次"变可见"事件）才生效，武装前可见性属性透传真实值、失焦事件正常下发
- 🐛 修复: 视频站**方向键快进时好时坏**（但 C/X/Z 倍速正常）
  - 根因：键盘监听注册太晚（扩展版在 `DOMContentLoaded`、油猴版更晚，要等找到视频之后）。同节点同阶段按注册顺序执行，站点播放器的 `keydown` 捕获监听先挂上并对方向键调 `stopImmediatePropagation()`，我们的 handler 根本不会被调用；C/X/Z 因站点不处理才幸免
  - 修复：两版均把 `keydown` 注册提到 `document_start` 同步执行，抢在站点脚本之前
- 🐛 修复: 部分页面控制台报 `Permissions policy violation: unload is not allowed in this document`，且栈顶指向本脚本
  - 根因：为拦截 7 个失焦事件而覆盖了 `EventTarget.prototype.addEventListener`，导致全页面每次监听注册都经过我们的函数，站点注册 `unload` 触发的警告被误报到我们身上
  - 修复：改为在 `window` 捕获阶段拦事件，不再覆盖原型方法。同时消除了拖慢所有监听注册、破坏 `once`/`AbortSignal` 原生语义的隐患
- 🐛 修复: 扩展版点播视频的方向键可能被永久禁用
  - 根因：直播判定只看首次 `canplay` 时的 `duration === Infinity`，而 MSE/流媒体播放器此刻常报 `Infinity`，之后才补上真实时长
  - 修复：改为等 `durationchange`（或 3s 超时）后仍为 `Infinity` 才判定为直播

### v2.2.2 (2026-09-06)
- ✨ 优化: Chrome 扩展 popup 面板默认倍速设置改用滑块（slider）UI
  - 可拖动滑块设置 0.25x ~ 4x 倍速，实时显示当前值
  - 渐变背景显示进度，交互更直观美观

### v2.2.1 (2026-09-06)
- 🐛 修复: Chrome 扩展在启用 Trusted Types 安全策略的网站（如 Google）上报错 "Failed to set the 'innerHTML' property"
  - 倍速控制 UI 创建改用 DOM API 替代 `innerHTML`，兼容严格的内容安全策略
- 🔧 重构: subtitle_backend/server.py 改用 ModelManager 统一管理语音识别模型
  - 现在与 system_audio_subtitle.py 使用相同架构，统一通过 model_config.json 配置模型
  - 支持 MLX Whisper (Apple Silicon 优化) / faster-whisper / SiliconFlow API 等多种后端
  - 当前配置默认使用 `mlx-community/whisper-large-v3-turbo`（效果更好）

### v2.2.0 (2026-09-06)
- ✨ 新增: **默认播放速度设置**功能
  - 可自定义视频初始播放速度（默认 1.5 倍，范围 0.1 ~ 16）
  - 油猴版：通过 Tampermonkey 菜单 "🎬 默认播放速度" 设置
  - 扩展版：通过扩展 popup 面板 "播放设置" 区域配置
  - 优先级：已记忆的倍速 > 默认倍速配置 > 1.0（当记忆倍速功能关闭时，使用默认倍速）
  - 按 Z 键切换加速状态时，若当前为 1 倍速，将恢复到记忆倍速或默认倍速

### v2.1.9 (2026-09-02)
- 🐛 修复: 在**没有实际视频**的页面上按 C/X 等快捷键也会弹“速度 xx”提示。根因有二：
  - 扩展版 `findVideo` 兜底逻辑过于激进，只要页面 DOM 里存在任意 `<video>`/`<audio>`（哪怕是隐藏的埋点/广告/预加载元素）就会被选为操作目标。修复：只接受“真正可用”的媒体（可见，或不可见但确实在播放），找不到则返回 `null`，绝不盲目返回第一个隐藏元素。
  - 部分平台把 GIF 动图用无声 `<video>` 实现（时长极短、无音轨、走 `tweet_video/` 等 GIF 专属路径），被脚本误当成正片视频。修复：新增 GIF 动图识别（URL 特征 + “无音轨/极短时长/无原生 controls/loop 或 autoplay”多重启发式），GIF 不再被选为倍速/快进等快捷键目标，真正的短视频不受影响。油猴与扩展版同步适配。

### v2.1.8 (2026-08-29)
- 🐛 修复: 部分自研播放器上按加/减速键（C/X）后速度提示一闪即被打回、无法真正倍速。定位到根因：这类播放器只接受离散档位（如 0.5/1/1.5/2，上限 2.0），会在自身 `ratechange` 处理里把脚本 +0.1 步进产生的"非法"速率（1.1、1.6、2.1 等）钳制回最近的合法档位。修复：在捕获阶段监听 `ratechange`，当实际速率已等于脚本期望值时 `stopImmediatePropagation`，阻止站点钳制回调执行，从而支持任意倍速。油猴与扩展版同步适配。

### v2.1.7 (2026-08-28)
- 🐛 修复: 部分网站上加速快捷键不响应。根因是站点在捕获阶段拦截了键盘事件，仅捕获阶段监听会被绕过；新增冒泡阶段监听作为兜底，双重保险。
- 🐛 修复: YouTube 等站点加速后，视频实际按新速率播放但倍速菜单仍显示"正常"/未同步。根因是这类播放器的菜单文案取自内部状态而非 video.playbackRate；新增站点 UI 同步器：
  - YouTube: 调用 `movie_player.setPlaybackRate` 更新内部状态（受官方档位限制，超过 2 倍时菜单显示 2，但实际速率仍由 guard 锁定为期望值）
  - 通用兜底: 探测 `window.__PLAYER__ / PLAYER / player`、`video._player / player / __player` 等常见播放器实例，尝试其 setPlaybackRate/playbackRate API，覆盖腾讯、B 站、xgplayer/dplayer/artplayer 等常见 H5 播放器
  - 事件兜底: 额外派发一次 `ratechange` 事件，应对通过事件驱动 UI 更新且 stopPropagation 了原生派发的场景
- 📦 油猴脚本与 Chrome 扩展版本同步升级至 2.1.7。

### v2.1.6 (2026-07-18)
- 🐛 修复: 部分网站（如自研播放器）加减速快捷键只能生效一次、按多次不变的问题。根因是这类播放器会在外部修改 playbackRate 后强制拉回内部记录值；新增 setPlaybackRate 守卫，在 video 实例上锁定期望速率，抵御站点拉回。油猴与扩展版同步适配。

### v2.1.5 (2026-07-11)
- ✨ 新增: 反失焦暂停——去除部分网站在鼠标移出窗口/切换标签时自动暂停视频的限制（伪装页面始终可见 + 拦截 visibilitychange/blur/pagehide 等失焦事件监听）
- ⚙️ 油猴脚本可通过菜单「本站关闭"反失焦暂停"」针对个别网站关闭；扩展可用 `localStorage.h5video_antiPause_off = '1'` 关闭

### v2.1.4 (2026-07-11)
- 🐛 修复: 播放控制进度条鼠标长时间不操作不自动隐藏（移除过于激进的 prototype setter 拦截，改为计数器策略：站点主动关闭 controls 超过 3 次后停止强制恢复，让站点自主管理显示/隐藏）

### v2.1.3 (2026-07-11)
- 🐛 修复: 原生全屏时倍速文字提示、倍速面板不显示（提示改为随全屏元素挂载进入 top layer）
- 🐛 修复: 站点已有 H5 自定义进度条时脚本额外多出一条原生进度条（仅在无自定义控制条时才强制开启原生 controls）
- 🎨 优化: 原生全屏改为对播放器容器全屏而非裸 `<video>`，保证提示可正常叠加显示
- 📦 油猴脚本与 Chrome 扩展版本号统一为 2.1.3

### v2.2.0 (2024-06-24)
- ✨ 新增: H5音频 (`<audio>`) 倍速控制支持
- ✨ 新增: 可视化倍速UI面板
- ✨ 新增: Q键显示/隐藏倍速面板
- ✨ 新增: E键微调加速快捷键
- 🎨 优化: 现代化毛玻璃UI设计
- 🎨 优化: 智能自动隐藏机制

### v2.1.2
- 原有视频播放控制功能

## 支持网站

- YouTube, Bilibili, 爱奇艺, 腾讯视频
- 优酷, 芒果TV, 西瓜视频
- Twitch, 斗鱼, 虎牙
- 所有使用HTML5 video/audio的网站

## 安装

### 油猴脚本
```
https://github.com/xixiU/awesome-script/blob/master/h5videoPlay/html5videoPlay.js
```

### Chrome扩展
加载 `chrome-extension` 目录

## 使用场景

- 📚 学习: 网课1.25x, 复习1.5x
- 🎬 娱乐: 追剧加速, 音乐学习慢放
- 💼 工作: 会议回放1.5x, 播客加速收听

## 常见问题

**Q: 倍速面板不显示?**  
A: 确认页面有视频/音频,按Q键手动显示,或刷新页面

**Q: 音频不支持倍速?**  
A: 确认是HTML5 audio元素,某些Flash播放器不支持

**Q: 快捷键冲突?**  
A: YouTube/抖音已自动重映射快捷键避免冲突
